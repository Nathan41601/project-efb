@model EnhancerForBusiness_Web.ViewModels.OrderDetailsViewModel

@{
    if (Model == null)
    {
        ViewBag.Title = "Orden desconocida";
    }
    else
    {
        ViewBag.Title = string.Format("Historial de pedidos para {0}", Model.Order.OrderId);
    }
}

@Html.Partial("_Order", Model)
