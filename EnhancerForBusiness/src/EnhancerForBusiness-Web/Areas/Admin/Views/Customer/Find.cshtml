@{
    ViewBag.Title = "Encontrar Clientes";
}

<h2>Búsqueda de clientes:</h2>

<div class="container">
    <div class="row clearfix">
        <div class="col-md-12 column">
            @using (Html.BeginForm("Find", "Customer", FormMethod.Get))
            {
                <div class="form-group">
                    <label for="username">Nombre de Usuario:</label>
                    <input class="form-control" id="username" name="Nombre de Usuario" type="text" />
                </div>
                    <div class="form-group">
                        <label for="email">Correo Electrónico:</label>
                        <input class="form-control" id="email" name="Correo Electrónico" type="email" />
                    </div>
                    <div class="form-group">
                        <label for="phoneNumber">Número de teléfono:</label>
                        <input class="form-control" id="phoneNumber" name="Número de Teléfono" type="number" />
                    </div>
                    <button class="btn btn-default" type="submit">Buscar</button>
            }
        </div>
    </div>
</div>